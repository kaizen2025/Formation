# Flask App Configuration
FLASK_APP=run.py
FLASK_DEBUG=True # Set to False in production
SECRET_KEY='your_very_secret_random_key_here' # Generate a strong random key (e.g., python -c 'import os; print(os.urandom(24))')

# Database Configuration
DATABASE_URL="postgresql://kwfqmcbp:qwhimeobjyxlsyuwhtyt@alpha.europe.mkdb.sh:5432/egdyfrvw" # Or your actual DB URL

# Email Configuration
MAIL_SERVER="outlook.office365.com"
MAIL_PORT=587
MAIL_USERNAME="kbivia@anecoop-france.com"
MAIL_PASSWORD="your_actual_email_password" # Keep this secure! Don't commit.
MAIL_USE_TLS=True
ADMIN_EMAILS="kbivia@anecoop-france.com" # Comma-separated if multiple

# Admin Password Hash (Generate using: python -c "from werkzeug.security import generate_password_hash; print(generate_password_hash('YourSecurePassword'))")
ADMIN_USERNAME="admin" # Or choose a different username
ADMIN_PASSWORD_HASH="pbkdf2:sha256:600000$yourGeneratedHashHere..."

# Optional CDN configuration (can still be in config.py if preferred)
# FONT_AWESOME_CDN="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
